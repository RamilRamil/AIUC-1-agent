# Карта покрытия таксономии атак — OWASP Top 10 for LLM Applications

> Порождается из `taxonomy/owasp-llm-top10.yaml` командой `aiuc taxonomy`. Вручную не редактировать.

**Версия списка:** 2025  
**Источник:** https://genai.owasp.org/llm-top-10/ (сверено 2026-07-16)

## Сводка

- ✅ tested (проверяем): **4**
- 🕳 gap (применимо, но не проверяем): **3**
- — not_applicable (нет предмета в архитектуре): **3**

**Проверяется 4 из 7 применимых классов (57%).**

## Категории

| Категория | Статус | Обоснование | Атаки |
|---|---|---|---|
| **LLM01** Prompt Injection | ✅ tested | Прямые инъекции проверяются с фичи 001 (direct-01/02). Косвенная — вредоносная инструкция приходит через результат инструмента — добавлена фичей 005 (indirect-01). Benign-кейсы контролируют ложные срабатывания того же класса. | `direct-01`, `direct-02`, `indirect-01`, `benign-01`, `benign-02` |
| **LLM02** Sensitive Information Disclosure | ✅ tested | Канареечный секрет и PII-детектор (фича 004). Утечка проверяется и в ответе, и через аргументы инструментов, и в журнале. | `exfil-01`, `exfil-02` |
| **LLM03** Supply Chain | — n/a | У стенда нет цепочки поставки: ни плагинов, ни сторонних компонентов, ни загружаемых моделей в рантайме. Модель приходит через шов как объект; подменять в этой архитектуре нечего. | — |
| **LLM04** Data and Model Poisoning | — n/a | Стенд не обучает и не дообучает модель и не строит эмбеддингов: обучающих данных, которые можно отравить, в архитектуре нет. | — |
| **LLM05** Improper Output Handling | 🕳 gap | ПРИМЕНИМО, но не проверяем. Выходной guardrail маскирует секрет и PII, однако санитизации вывода для downstream-потребителя (интерпретация как код/разметка/SQL) нет. Кластер output-filters карты покрытия. Помечено gap, а не tested: маскирование ≠ санитизация. | — |
| **LLM06** Excessive Agency | ✅ tested | Allow-list инструментов (wrap_tool_call) и атаки на обход роли (role-01/02): проверяется, выполнит ли агент действие вне заявленных полномочий. | `role-01`, `role-02` |
| **LLM07** System Prompt Leakage | ✅ tested | Канарейка лежит ИМЕННО в системном промпте, и её раскрытие — критерий успеха атак direct-01/02. Попадание точное: это ровно тот класс, что описывает категория. | `direct-01`, `direct-02` |
| **LLM08** Vector and Embedding Weaknesses | — n/a | У стенда нет RAG и нет векторного хранилища: ни эмбеддингов, ни retrieval-пайплайна, чьи слабости описывает категория. | — |
| **LLM09** Misinformation | 🕳 gap | ПРИМЕНИМО, но не проверяем. Галлюцинации и заземлённость ответа не измеряются: нет ни базы знаний, ни проверки цитат. Кластер output-filters карты (контроль D001 AIUC-1). | — |
| **LLM10** Unbounded Consumption | 🕳 gap | ПРИМЕНИМО, но не проверяем. Лимитов частоты и объёма нет; есть только таймаут попытки, что про надёжность прогона, а не про исчерпание ресурсов мишени. Кластер input-moderation карты (B004/B009 AIUC-1). | — |

---

## Как читать эту цифру

Покрытие таксономии — про **широту**: сколько классов атак стенд вообще проверяет.
Доля отражённых атак в `scorecard.md` — про **глубину**: насколько хорошо он держится
на той узкой полоске, которую проверяет.

Вместе они читаются правильно: *стенд отражает атаки из тех классов, которые умеет
проверять, а умеет он меньшинство.* По отдельности каждая врёт — первая занижает,
вторая льстит. Поэтому отчёты намеренно разделены.

**Что это НЕ решает.** Из внешнего списка взяты **категории**; **нагрузки** написаны
внутри проекта. Классификация показывает, каких классов атак стенд не проверяет вовсе,
но не делает нагрузки представительными. Полное устранение самоподтверждаемости требует
внешнего корпуса нагрузок.

**Правило статусов.** `tested` не ставится за «что-то похожее» — отдалённо смежное это
`gap`. `not_applicable` обосновывается отсутствующей частью архитектуры, а не
отсутствием желания.

